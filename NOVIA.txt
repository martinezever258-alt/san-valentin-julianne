<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Para Julianne ❤️</title>
  <meta name="description" content="Una sorpresa especial para ti, Julianne ❤️">
  <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 100 100%22><text y=%22.9em%22 font-size=%2290%22>❤️</text></svg>">
  <style>
    :root{
      --rosa:#ff4d6d;
      --rosa-2:#ff758f;
      --gris:#594a4e;
      --fondo:#fff0f3;
      --bg-blanco:#ffffff;
      --sombra: 0 15px 35px rgba(255, 77, 109, 0.2);
    }
    *{box-sizing:border-box}
    html,body{height:100%}
    body{
      margin:0; padding:0;
      display:flex; justify-content:center; align-items:center;
      background: radial-gradient(1200px 1200px at 80% -10%, rgba(255,77,109,0.08), transparent 60%), var(--fondo);
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      overflow:hidden; text-align:center;
      color:var(--gris);
    }

    .container{
      background:var(--bg-blanco);
      padding:36px 28px;
      border-radius:28px;
      box-shadow:var(--sombra);
      width:min(92vw, 450px);
      position:relative; z-index:10;
      border:1px solid rgba(255,77,109,0.08);
      backdrop-filter: blur(2px);
    }
    h1{color:var(--rosa); font-size:2.1rem; margin:0 0 8px}
    p{margin:8px 0 0; line-height:1.6}
    .lead{font-size:1.05rem}
    .pregunta{margin-top:6px; font-size:1.2rem; font-weight:700}

    .buttons{
      margin-top:26px; display:flex; justify-content:center; gap:18px;
      height:64px; align-items:center; position:relative;
    }
    button{
      padding:12px 26px; font-size:1.05rem; font-weight:700;
      border:none; border-radius:999px; cursor:pointer;
      transition: transform .18s ease, background-color .2s ease, box-shadow .2s ease;
      user-select:none;
    }
    #btnSi{ background:var(--rosa); color:#fff; box-shadow:0 8px 18px rgba(255,77,109,.4) }
    #btnSi:hover{ transform:translateY(-1px) scale(1.04); background:var(--rosa-2) }
    #btnNo{
      background:#e9ecef; color:#495057; position:absolute; inset:auto auto auto auto;
      box-shadow:0 6px 14px rgba(73,80,87,.18)
    }

    .oculto{display:none}

    /* Corazones animados */
    .heart{
      position:fixed; color:var(--rosa); font-size:24px; pointer-events:none; z-index:1;
      animation: floatUp linear forwards;
      text-shadow: 0 2px 8px rgba(255,77,109,.35);
      will-change: transform, opacity;
    }
    @keyframes floatUp{
      0%{ transform: translateY(100vh) scale(1) rotate(0deg); opacity: .95; }
      100%{ transform: translateY(-12vh) scale(1.2) rotate(360deg); opacity: 0; }
    }

    /* Mensaje final */
    #mensajeFinal h1{ font-size:2.2rem; margin-bottom:6px }
    .typewriter{
      display:inline-block;
      border-right:2px solid var(--rosa);
      white-space:wrap; overflow:hidden;
      animation: blink .9s step-end infinite;
      max-width: 100%;
    }
    @keyframes blink{ 50%{ border-color: transparent } }

    /* Barra superior: música */
    .topbar{
      position:fixed; top:12px; left:50%; transform:translateX(-50%);
      background:rgba(255,255,255,.9); border:1px solid rgba(255,77,109,.15);
      box-shadow:0 8px 24px rgba(0,0,0,.06); padding:8px 14px; border-radius:999px; z-index:20;
      display:flex; gap:10px; align-items:center; backdrop-filter: blur(6px);
    }
    .topbar button{
      padding:8px 14px; font-size:.95rem; border-radius:999px;
      background:#ffe3e8; color:#b3003c;
    }
    .topbar button:hover{ background:#ffd6de }

    /* Confeti */
    .confetti{
      position:fixed; top:-10px; width:10px; height:14px; opacity:.9; z-index:5;
      animation: fall linear forwards;
      will-change: transform, opacity;
      border-radius:2px;
    }
    @keyframes fall{
      to{
        transform: translateY(110vh) rotate(720deg);
        opacity: .95;
      }
    }

    /* Responsive ajustes */
